<h1>Expenditures</h1>

<p>Total Amount for <%= @category.name %>: <%= @category.expenditures.sum(:amount) %></p>

<% @expenditures.each do |expenditure| %>
  <div>
    <%= image_tag expenditure.icon, class: "img white" %>
    <p>Amount: <%= expenditure.amount %></p>
  </div>
<% end %>

<%= link_to "Add a new expenditure", new_category_expenditure_path(@category) %>
<%= link_to "Back", categories_path %>